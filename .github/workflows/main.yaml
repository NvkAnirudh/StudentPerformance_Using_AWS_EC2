name: workflow

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'README.md'

permissions:
  id-token: write
  contents: read

jobs:
  integration:
    name: Continuous Integration
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Lint Code
        run: echo "Linting repository"

      - name: Run unit tests
        run: echo "Running unit tests"
  
  # Docker images can be publicly published onto Docker Hub. But, in companies, these images are meant to be private, so they push the images onto AWS ECR (Fully-managed Docker Container to Store, Manage,
  # and Deploy Images)
  build-and-push-ecr-image:
